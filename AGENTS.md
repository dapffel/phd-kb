# PhD Knowledge Base — Agent Instructions

The canonical workflow instructions for this template live in [CLAUDE.md](CLAUDE.md).

Before changing files in this vault, read `CLAUDE.md` and follow it as the source of truth. It defines the project structure, ownership rules for `raw/`, `wiki/`, and `research/`, and the workflows for prompts such as `catalog`, `ingest`, `compile-concepts`, `query`, `synthesize`, `eval`, and `lint`.

For tools that automatically load `AGENTS.md` but not `CLAUDE.md`, this file is the bridge: use `CLAUDE.md` as the full instruction set.
