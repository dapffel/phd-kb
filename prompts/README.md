# prompts/

These prompt templates are the reusable logic for the knowledge base workflows. Edit them carefully, because changes here affect future summaries, concept articles, syntheses, evaluations, and lint reports. After changing a prompt, run the affected workflow on a small example first and review the diff before regenerating many files.
