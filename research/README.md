# research/

This directory is for your dissertation chapters and original synthesis. Draft chapter files are ignored by Git by default so your early thinking stays local; remove or narrow the `research/*` ignore rule in `.gitignore` when you are ready to version a chapter. Starter templates live in `research/templates/` so every copy of this template repo includes the expected chapter structure.
